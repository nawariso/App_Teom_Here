{
  "name": "toem-here-firebase-tests",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "firebase emulators:exec --config ../firebase.json --project toem-here-test --only firestore,storage \"vitest run --no-file-parallelism\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^15.24.0",
    "vitest": "^4.1.10"
  }
}
